apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: external-app
spec:
  hosts:
  - external-app.corp.net
  location: MESH_EXTERNAL
  ports:
  - number: 80
    name: http
    protocol: HTTP
  - number: 443
    name: tls
    protocol: TLS
  resolution: DNS
